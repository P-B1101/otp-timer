part of '../../otp_timer.dart';

class OtpUtils {
  const OtpUtils._();

  /// Use to change timer duration. for example when you want to
  /// update timer duration base on response.
  ///
  static Duration timerDuration = const Duration(seconds: 180);
}
